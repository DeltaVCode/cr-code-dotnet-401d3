@page
@model TodoApi.Pages.IndexModel
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@foreach (var todo in Model.Todos)
{
    <li>
        @*@await Html.PartialAsync("_DisplayTodo", todo)*@
        <partial name="_DisplayTodo" model="todo" />
    </li>
}

@await Component.InvokeAsync("Username") 
